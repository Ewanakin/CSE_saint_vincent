{% extends 'base.html.twig' %}

{% block title %}Hello QuestionController!{% endblock %}

{% block body %}
    <div class="h-100 flex flex-col items-center h-100 mt-16">
        <div class="flex flex-col justify-center mt-8">
            {{ form_start(questionForm) }}
                {{ form_row(questionForm.question) }}
                <button type="submit">submit</button>
            {{ form_end(questionForm) }}
        </div>
        <a href="{{ path('reponse_list', {question: question.id}) }}" class="cursor-pointer hover:font-bold">Voir les réponses du sondage</a>
        <a href="{{ path('question_list') }}" class="cursor-pointer hover:font-bold">Retourner à la gestion des Sondages</a>
    </div>
{% endblock %}
