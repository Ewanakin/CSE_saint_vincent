{% extends 'base.html.twig' %}

{% block title %}Navbar{% endblock %}

{% block body %}
<style>
    body {
        margin: 0;
        font-family: Arial, Helvetica, sans-serif;
    }

    .topnav {
        overflow: hidden;
        background-color: #384394;
        padding-right: 420px;
        height: 70px;
        align-items: center;
    }

    .topnav a {
        float: right;
        color: #f2f2f2;
        text-align: center;
        padding: 27px 19px;
        text-decoration: none;
        font-size: 17px;
        padding-top: 23px;
    }

    .topnav a:hover {
        background-color: #CCC65D;
    }

    .topnav a.active {
        background-color: #04AA6D;
        color: white;
    }

    .logo-image {
        background-color: white;
        width: 17%;
        margin-left: 200px;
        padding-left: 60px;
        padding-top: 20px;
        padding-bottom: 81px;
        height: 40px;
        position: absolute;
    }

    .topG {
        background-color: lightgray;
        width: 100%;
        height: 70px;
    }
</style>
        <div class="logo-image">
            <img src="../../../img/Logo-Saint-Vincent.png" alt="logo">
        </div>
        <div class="topG"></div>
        <div class="topnav">
            <a href="#about">Contact</a>
            <a href="{{ path('show_offers') }}">Billeterie</a>
            <a href="{{ path('app_about') }}">A Propos de Nous</a>
            <a href="{{ path('partner_list') }}">Partenariats</a>
            <a href="{{ path('app_page_accueil') }}">Accueil</a>
        </div>
{% endblock %}
